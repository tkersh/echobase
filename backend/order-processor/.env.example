# Order Processor Environment Configuration
#
# NOTE: When running with Docker Compose, credentials are provided from the root .env file
# This file is only needed for local development outside of Docker
#
# For Docker deployment: Use the root .env file and run ./generate-credentials.sh
# For local development: Copy this file to .env and configure the values below

# AWS/Localstack Configuration
AWS_REGION=us-east-1

# For Localstack local development
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
SQS_ENDPOINT=http://localhost:4566
SQS_QUEUE_URL=http://sqs.us-east-1.localhost.localstack.cloud:4566/000000000000/order-processing-queue

# For production AWS deployment
# DO NOT hardcode credentials - use IAM roles or AWS Secrets Manager
# AWS_ACCESS_KEY_ID=<use-iam-role>
# AWS_SECRET_ACCESS_KEY=<use-iam-role>
# SQS_ENDPOINT=<not-needed-for-real-aws>
# SQS_QUEUE_URL=https://sqs.us-east-1.amazonaws.com/YOUR_ACCOUNT_ID/order-processing-queue

# Database Configuration
# SECURITY WARNING: Change these to strong random passwords
# Use the root ./generate-credentials.sh script to generate secure credentials
DB_HOST=localhost
DB_PORT=3306
DB_USER=orderuser
DB_PASSWORD=CHANGE_ME_TO_STRONG_RANDOM_PASSWORD
DB_NAME=orders_db

# Processing Configuration
POLL_INTERVAL=5000
MAX_MESSAGES=10